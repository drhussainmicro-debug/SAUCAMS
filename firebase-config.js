/* ===========================================================================
   SAUC AMS — Firebase configuration

   Filled in for the "saucams" project. Nothing here needs editing.

   Both index.html and admin.html read this file, so if you ever move to a
   different Firebase project, this is the only file you change.

   It is fine that these values are public. A Firebase web config identifies
   the project; it does not grant access. Access is controlled by
   firestore.rules — which is why publishing those rules matters.
   =========================================================================== */

window.__SAUC_FIREBASE = {
  apiKey:            "AIzaSyB-lWp1vTW2Xv_46TUjtw0ixY_d_-Ax4uM",
  authDomain:        "saucams.firebaseapp.com",
  projectId:         "saucams",
  storageBucket:     "saucams.firebasestorage.app",
  messagingSenderId: "451213882445",
  appId:             "1:451213882445:web:b0506fd7c8b2f6b324d804"
};

/* measurementId (G-G0KSZZS4S7) is deliberately left out: it is only used by
   Google Analytics, which this app does not load. */
